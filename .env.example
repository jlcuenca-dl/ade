# .env.example — Variables de entorno para desarrollo local
# Copiar como .env y ajustar valores:  cp .env.example .env

# ── Base de Datos ──
DB_PASSWORD=ade_local_2026

# ── pgAdmin (opcional) ──
PGADMIN_PASSWORD=admin2026

# ── Backend ──
ENV=local
